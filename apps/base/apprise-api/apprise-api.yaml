apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: apprise-api
spec:
  releaseName: apprise-api
  chart:
    spec:
      chart: ./charts/onechart
      reconcileStrategy: Revision
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  interval: 12h
  valuesFrom:
    - kind: ConfigMap
      name: apprise-api-helm-values
      valuesKey: apprise-api-values.yaml
      optional: true
  values:
    image:
      repository: lscr.io/linuxserver/apprise-api # {"$imagepolicy": "arkham:apprise-api-policy:name"}
      tag: "version-v1.5.1" # {"$imagepolicy": "arkham:apprise-api-policy:tag"}
    vars:
      PUID: ${PUID:=1000}
      PGID: ${PGID:=1000}
      TZ: ${TZ:=Etc/UTC}
    podSecurityContext:
      fsGroup: ${PGID:=1000}
    volumes:
      - name: config
        size: "100Mi"
        path: /config
        subPath: apprise-api/config
        existingClaim: pvc-storage
    containerPort: 8000
    probe:
      enabled: true
      path: /status
      settings:
        initialDelaySeconds: 5
        timeoutSeconds: 5
    livenessProbe:
      enabled: true
      path: /status
      settings:
        initialDelaySeconds: 5
        timeoutSeconds: 10
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: apprise-api
spec:
  image: lscr.io/linuxserver/apprise-api
  interval: 24h
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: apprise-api-policy
spec:
  imageRepositoryRef:
    name: apprise-api
  filterTags:
    pattern: '^version-v(?P<major>0|[1-9]\d*)\.(?P<minor>0|[1-9]\d*)\.(?P<patch>0|[1-9]\d*)$'
    extract: "$major.$minor.$patch"
  policy:
    semver:
      range: 1.x
  digestReflectionPolicy: Always
  interval: 24h
