---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: error-pages-policy
spec:
  imageRepositoryRef:
    name: error-pages
  policy:
    semver:
      range: 3.x
  digestReflectionPolicy: Always
  interval: 24h
