apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: registry

resources:
  - namespace.yaml
  - ../../base/registry

configMapGenerator:
  - name: registry-helm-values
    literals: []
    options:
      disableNameSuffixHash: true
