apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# airflow is cataloged but not currently deployed in kyrion.
# Sealed secrets are kept here so they are available when
# airflow is enabled by adding ../../base/airflow to resources.
resources:
  - airflow-webserver-secret-key-sealed.yaml
