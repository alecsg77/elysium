apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: coder-tls
  namespace: coder
spec:
  secretName: coder-tls
  dnsNames:
    - coder.${PRIVATE_DOMAIN}
    - "*.coder.${PRIVATE_DOMAIN}"
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
