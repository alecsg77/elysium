apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: headlamp
  namespace: kube-system
spec:
  releaseName: headlamp
  chart:
    spec:
      chart: headlamp
      sourceRef:
        kind: HelmRepository
        name: headlamp
        namespace: kube-system
  interval: 12h
  values:
    automountServiceAccountToken: false
    clusterRoleBinding:
      create: false
    config:
      inCluster: true
      inClusterContextName: kyrion
      pluginsDir: /build/plugins
      tlsCertPath: /etc/headlamp/tls/tls.crt
      tlsKeyPath: /etc/headlamp/tls/tls.key
      unsafeUseServiceAccountToken: false
      oidc:
        callbackURL: https://headlamp.${PRIVATE_DOMAIN}/oidc-callback
        externalSecret:
          enabled: true
          hasScopes: true
          name: headlamp-oidc
        secret:
          create: false
        useAccessToken: false
        usePKCE: true
    env:
      - name: OIDC_CALLBACK_URL
        value: https://headlamp.${PRIVATE_DOMAIN}/oidc-callback
      - name: OIDC_USE_PKCE
        value: "true"
    service:
      port: 443
      annotations:
        external-dns.alpha.kubernetes.io/hostname: headlamp.${PRIVATE_DOMAIN}
    probes:
      scheme: HTTPS
    initContainers:
      - name: headlamp-flux-plugin
        image: ghcr.io/headlamp-k8s/headlamp-plugin-flux:latest
        imagePullPolicy: Always
        command:
          - /bin/sh
          - -c
          - mkdir -p /build/plugins && cp -r /plugins/* /build/plugins/ && chown -R 100:101 /build
        securityContext:
          runAsNonRoot: false
          runAsUser: 0
          runAsGroup: 0
        volumeMounts:
          - name: headlamp-plugins
            mountPath: /build/plugins
    volumeMounts:
      - name: headlamp-plugins
        mountPath: /build/plugins
      - name: headlamp-tls
        mountPath: /etc/headlamp/tls
        readOnly: true
    volumes:
      - name: headlamp-plugins
        emptyDir: {}
      - name: headlamp-tls
        secret:
          secretName: headlamp-tls
    resources:
      requests:
        cpu: 50m
        memory: 128Mi
      limits:
        memory: 256Mi
