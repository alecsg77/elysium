apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: flux-web-tls
  namespace: flux-system
spec:
  secretName: flux-web-tls
  dnsNames:
    - flux.${PRIVATE_DOMAIN}
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
