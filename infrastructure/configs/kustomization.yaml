apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ts-dns.yaml
  - k3s-upgrade.yaml

configMapGenerator:
  - name: coredns-custom
    namespace: kube-system
    options:
      disableNameSuffixHash: true
    files:
      - ts.net.server